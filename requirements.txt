streamlit>=1.35.0
requests>=2.31.0
sentence-transformers>=2.7.0
faiss-cpu>=1.8.0
anthropic>=0.40.0
python-dotenv>=1.0.0

# wiki-rag's rag.py imports `langchain.docstore.document.Document`, an API removed
# in LangChain 1.0 (Oct 2025). Pin to the last pre-1.0 line so pip's resolver
# doesn't grab langchain-community 0.4.x (built for the 1.x architecture) and
# break the import. See: https://github.com/RoyRin/wiki-rag
langchain==0.3.27
langchain-community==0.3.27

git+https://github.com/RoyRin/wiki-rag.git
